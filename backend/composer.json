{
  "name": "deskbot/backend",
  "description": "Secure PHP backend for DeskBot: proxies AI chat requests and stores memory, so no API key ever ships inside the mobile app.",
  "type": "project",
  "require": {
    "php": ">=8.1",
    "vlucas/phpdotenv": "^5.6"
  },
  "autoload": {
    "psr-4": { "DeskBot\\": "src/" }
  }
}
